# ViHalluMT - phu thuoc loi (chay duoc tren Colab / Kaggle / may ca nhan)
# Dung khoang phien ban thay vi ghim cung, vi Colab va Kaggle cap nhat torch
# theo lich rieng. Cac phien ban da kiem chung o cuoi file.

# --- Nen tang ---
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# --- Mo hinh & du lieu ---
torch>=2.1
transformers>=4.44
datasets>=2.19
sentence-transformers>=3.0
accelerate>=0.30
huggingface-hub>=0.24

# --- Luong tu hoa (chi can khi chay LLM 7B tren T4) ---
bitsandbytes>=0.43 ; platform_system != "Darwin"

# --- Tieng Viet ---
underthesea>=6.8

# --- Demo ---
gradio>=4.0

# --- Phat trien ---
pytest>=7.4

# ---------------------------------------------------------------------------
# Phien ban da kiem chung tren may phat trien (2026-09-01):
#   Python 3.14.3, torch 2.13.0, transformers 5.6.2, datasets 4.8.5,
#   sentence-transformers 5.4.1, pandas 3.0.2, numpy 2.4.4,
#   scikit-learn 1.8.0, scipy 1.17.1
#
# Phan tai lap HalOmi (scripts/eda_halomi.py, scripts/replicate_p1_baselines.py)
# KHONG can GPU va KHONG can torch - chi can pandas + scikit-learn + scipy.
# ---------------------------------------------------------------------------
